[project]
name = "lumina-activity-classifier"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "numpy>=2.0",
  "pillow>=12.2",
  "pydantic>=2.13",
  "torch>=2.12",
  "torchvision>=0.27",
  "lumina-shared",
]

[tool.setuptools.packages.find]
where = ["src"]
